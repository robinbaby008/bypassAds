{
  "name": "bypassads",
  "version": "1.0.0",
  "description": "Shortlink Bypass — static installer + bypass API (Vercel or Render)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "undici": "^8.10.2"
  }
}
