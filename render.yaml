# Render Blueprint — single Web Service (static site + bypass API).
# Deploy: Render Dashboard → New → Blueprint → select this repo.
services:
  - type: web
    name: bypassads
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: BYPASS_WAIT_MS
        value: 8000
      # Residential proxy for upstream requests (paste key in Dashboard, never in git).
      - key: HTTP_PROXY
        sync: false
